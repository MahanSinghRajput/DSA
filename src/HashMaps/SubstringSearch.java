package HashMaps;
import java.util.*;

// Karp-Rabin String Matching Algorithm
// Time Complexity = O(len(a) + len(b) * cost(hash function)).
//                   O(len(a) * len(b)) (worst case)
public class SubstringSearch {
    public static void main(String[] args) {

    }
}
