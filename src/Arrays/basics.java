package Arrays;

public class basics {
    public static void main(String[] args) {
        // datatype[] variable_name = new datatype[size];
        /*
            for each loop:
            Syntax
            for(int num: arr){
                System.out.println(num); // you can get the values at every index.
            }

            to print arrays in brackets or just to print use:
            System.out.println(Arrays.toString(arr));
         */

        /*
        String[] str = new String[4];
        str[0] ="aa";
        str[1]="bb";
        str[2]="cc";
        str[3]="dd";
        System.out.println(Arrays.toString(str));
        str[1] = "Mahan";
        System.out.println(Arrays.toString(str));
         */

    }
}
